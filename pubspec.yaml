name: star_trails
description: Star Trails - Educational space trading adventure for elementary students
publish_to: 'none'
# Version is managed by the edition/versioning system in core/version_manager/
# See VERSIONING_SYSTEM_SPECIFICATION.md for details
# Default version for development builds
version: 1.0.1+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  crypto: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 6.0.0

flutter:
  uses-material-design: true
  assets:
    - config/version_config.json
    - config/editions.json
